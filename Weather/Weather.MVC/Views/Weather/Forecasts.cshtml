@model IEnumerable<Weather.Domain.Entities.Forecast>

@{
    ViewBag.Title = "Forecasts";
}

<h2>Forecasts</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Location.Lat)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Temperature)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cloudiness)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NederBird)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SymbolId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Location.Lat)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Temperature)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cloudiness)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NederBird)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SymbolId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
